<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>NetworkVerify</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Battle\Battle.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Battle 脚本层：进程内启动需要 scripts 目录 -->
    <None Include="..\..\GameLogic\scripts\*.csx" Link="scripts\%(Filename)%(Extension)" CopyToOutputDirectory="Always" />
  </ItemGroup>

</Project>
