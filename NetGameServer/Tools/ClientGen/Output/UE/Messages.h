// <auto-generated> 由 ClientGen 生成，请勿手动修改。
#pragma once
// 客户端消息定义（与服务器 Framework.Protocol.Generated 二进制兼容，MemoryPack 格式）。
#include <cstdint>
#include <map>
#include <string>
#include <vector>
#include "MemoryPack.h"

namespace ClientProtocol
{
/// <summary>结构体 Vector3（3 个成员）。</summary>
struct Vector3
{
    float X = 0.0f;
    float Y = 0.0f;
    float Z = 0.0f;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteFloat(X);
        w.WriteFloat(Y);
        w.WriteFloat(Z);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        X = r.ReadFloat();
        Y = r.ReadFloat();
        Z = r.ReadFloat();
    }
};

/// <summary>结构体 EntityState（8 个成员）。</summary>
struct EntityState
{
    int64_t EntityId = 0;
    std::string Nickname;
    Vector3 Position;
    Vector3 Rotation;
    int32_t Hp = 0;
    int32_t MaxHp = 0;
    int32_t Score = 0;
    std::vector<int32_t> Equipment;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(EntityId);
        w.WriteString(Nickname);
        w.WriteHeader(3); Position.WriteBody(w);
        w.WriteHeader(3); Rotation.WriteBody(w);
        w.WriteI32(Hp);
        w.WriteI32(MaxHp);
        w.WriteI32(Score);
        w.WriteCount((int32_t)Equipment.size()); for (const auto& it : Equipment) { w.WriteI32(it); }
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        EntityId = r.ReadI64();
        Nickname = r.ReadString();
        r.SkipHeader(); Position.ReadBody(r);
        r.SkipHeader(); Rotation.ReadBody(r);
        Hp = r.ReadI32();
        MaxHp = r.ReadI32();
        Score = r.ReadI32();
        int32_t _n = r.ReadCount(); Equipment.clear(); Equipment.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { int32_t _e = r.ReadI32(); Equipment.push_back(_e); }
    }
};

/// <summary>结构体 PlayerInput（4 个成员）。</summary>
struct PlayerInput
{
    int64_t InputId = 0;
    int32_t Buttons = 0;
    float MoveX = 0.0f;
    float MoveY = 0.0f;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(InputId);
        w.WriteI32(Buttons);
        w.WriteFloat(MoveX);
        w.WriteFloat(MoveY);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        InputId = r.ReadI64();
        Buttons = r.ReadI32();
        MoveX = r.ReadFloat();
        MoveY = r.ReadFloat();
    }
};

/// <summary>结构体 RoomInfo（12 个成员）。</summary>
struct RoomInfo
{
    std::string RoomId;
    std::string RoomName;
    std::string SceneId;
    std::string SceneType;
    std::string BattleNodeId;
    int32_t OwnerUserId = 0;
    bool IsPrivate = false;
    bool HasPassword = false;
    int32_t MaxPlayers = 0;
    int32_t CurrentPlayers = 0;
    std::string RoomStatus;
    int64_t CreatedAtUtc = 0;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(BattleNodeId);
        w.WriteI32(OwnerUserId);
        w.WriteBool(IsPrivate);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(RoomStatus);
        w.WriteI64(CreatedAtUtc);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        BattleNodeId = r.ReadString();
        OwnerUserId = r.ReadI32();
        IsPrivate = r.ReadBool();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        RoomStatus = r.ReadString();
        CreatedAtUtc = r.ReadI64();
    }
};

/// <summary>结构体 FriendInfo（5 个成员）。</summary>
struct FriendInfo
{
    int32_t FriendUserId = 0;
    std::string FriendUniqueId;
    std::string Nickname;
    std::string Remark;
    bool IsOnline = false;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(FriendUserId);
        w.WriteString(FriendUniqueId);
        w.WriteString(Nickname);
        w.WriteString(Remark);
        w.WriteBool(IsOnline);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        FriendUserId = r.ReadI32();
        FriendUniqueId = r.ReadString();
        Nickname = r.ReadString();
        Remark = r.ReadString();
        IsOnline = r.ReadBool();
    }
};

/// <summary>结构体 BlacklistInfo（4 个成员）。</summary>
struct BlacklistInfo
{
    int32_t BlockedUserId = 0;
    std::string BlockedUniqueId;
    std::string BlockedNickname;
    int64_t AddTime = 0;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(BlockedUserId);
        w.WriteString(BlockedUniqueId);
        w.WriteString(BlockedNickname);
        w.WriteI64(AddTime);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        BlockedUserId = r.ReadI32();
        BlockedUniqueId = r.ReadString();
        BlockedNickname = r.ReadString();
        AddTime = r.ReadI64();
    }
};

/// <summary>结构体 FriendApplyInfo（7 个成员）。</summary>
struct FriendApplyInfo
{
    int64_t ApplyId = 0;
    int32_t RequesterUserId = 0;
    std::string RequesterUniqueId;
    std::string RequesterNickname;
    std::string Message;
    std::string Status;
    int64_t CreateTimeUtc = 0;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(ApplyId);
        w.WriteI32(RequesterUserId);
        w.WriteString(RequesterUniqueId);
        w.WriteString(RequesterNickname);
        w.WriteString(Message);
        w.WriteString(Status);
        w.WriteI64(CreateTimeUtc);
    }

    /// <summary>读取成员数据（不含对象头）。</summary>
    void ReadBody(mp::Reader& r)
    {
        ApplyId = r.ReadI64();
        RequesterUserId = r.ReadI32();
        RequesterUniqueId = r.ReadString();
        RequesterNickname = r.ReadString();
        Message = r.ReadString();
        Status = r.ReadString();
        CreateTimeUtc = r.ReadI64();
    }
};

/// <summary>40001 BattleJoin -> Battle（服务器目标 Battle）。</summary>
struct BattleJoin
{
    static constexpr int32_t MsgId = 40001;
    static constexpr const char* Target = "Battle";
    std::string RoomId;
    std::string SceneName;
    std::string SceneType;
    int32_t MaxPlayers = 0;
    std::map<std::string, std::string> CustomRules;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(5);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(SceneName);
        w.WriteString(SceneType);
        w.WriteI32(MaxPlayers);
        w.WriteCount((int32_t)CustomRules.size()); for (const auto& kv : CustomRules) { w.WriteString(kv.first); w.WriteString(kv.second); }
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        SceneName = r.ReadString();
        SceneType = r.ReadString();
        MaxPlayers = r.ReadI32();
        int32_t _n = r.ReadCount(); CustomRules.clear(); for (int32_t _i = 0; _i < _n; ++_i) { std::string _k = r.ReadString(); std::string _v = r.ReadString(); CustomRules[_k] = _v; }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BattleJoin Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BattleJoin m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40002 BattleJoinResult -> Battle（服务器目标 Battle）。</summary>
struct BattleJoinResult
{
    static constexpr int32_t MsgId = 40002;
    static constexpr const char* Target = "Battle";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BattleJoinResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BattleJoinResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40003 BattleFrameSync -> Battle（服务器目标 Battle）。</summary>
struct BattleFrameSync
{
    static constexpr int32_t MsgId = 40003;
    static constexpr const char* Target = "Battle";
    int32_t FrameId = 0;
    std::vector<PlayerInput> Inputs;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(FrameId);
        w.WriteCount((int32_t)Inputs.size()); for (const auto& it : Inputs) { w.WriteHeader(4); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        FrameId = r.ReadI32();
        int32_t _n = r.ReadCount(); Inputs.clear(); Inputs.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { PlayerInput _e; r.SkipHeader(); _e.ReadBody(r); Inputs.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BattleFrameSync Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BattleFrameSync m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40004 BattleLeaveRoom -> Battle（服务器目标 Battle）。</summary>
struct BattleLeaveRoom
{
    static constexpr int32_t MsgId = 40004;
    static constexpr const char* Target = "Battle";
    std::string RoomId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BattleLeaveRoom Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BattleLeaveRoom m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40005 BattleLeaveRoomResult -> Battle（服务器目标 Battle）。</summary>
struct BattleLeaveRoomResult
{
    static constexpr int32_t MsgId = 40005;
    static constexpr const char* Target = "Battle";
    bool Success = false;
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BattleLeaveRoomResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BattleLeaveRoomResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40006 ScriptAction -> Battle（服务器目标 Battle）。</summary>
struct ScriptAction
{
    static constexpr int32_t MsgId = 40006;
    static constexpr const char* Target = "Battle";
    int64_t EntityId = 0;
    std::string Method;
    std::vector<int32_t> Args;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(EntityId);
        w.WriteString(Method);
        w.WriteCount((int32_t)Args.size()); for (const auto& it : Args) { w.WriteI32(it); }
    }

    void ReadBody(mp::Reader& r)
    {
        EntityId = r.ReadI64();
        Method = r.ReadString();
        int32_t _n = r.ReadCount(); Args.clear(); Args.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { int32_t _e = r.ReadI32(); Args.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ScriptAction Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ScriptAction m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40010 ClientTimeSync -> Battle（服务器目标 Battle）。</summary>
struct ClientTimeSync
{
    static constexpr int32_t MsgId = 40010;
    static constexpr const char* Target = "Battle";
    int64_t ClientSendMs = 0;
    int64_t LastServerSendMs = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(ClientSendMs);
        w.WriteI64(LastServerSendMs);
    }

    void ReadBody(mp::Reader& r)
    {
        ClientSendMs = r.ReadI64();
        LastServerSendMs = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ClientTimeSync Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ClientTimeSync m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40011 ServerTimeSync -> Battle（服务器目标 Battle）。</summary>
struct ServerTimeSync
{
    static constexpr int32_t MsgId = 40011;
    static constexpr const char* Target = "Battle";
    int64_t ClientSendMs = 0;
    int64_t ServerRecvMs = 0;
    int64_t ServerSendMs = 0;
    int64_t AuthFrame = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(4);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(ClientSendMs);
        w.WriteI64(ServerRecvMs);
        w.WriteI64(ServerSendMs);
        w.WriteI64(AuthFrame);
    }

    void ReadBody(mp::Reader& r)
    {
        ClientSendMs = r.ReadI64();
        ServerRecvMs = r.ReadI64();
        ServerSendMs = r.ReadI64();
        AuthFrame = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ServerTimeSync Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ServerTimeSync m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40101 EntitySync -> Battle（服务器目标 Battle）。</summary>
struct EntitySync
{
    static constexpr int32_t MsgId = 40101;
    static constexpr const char* Target = "Battle";
    Vector3 Position;
    Vector3 Rotation;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteHeader(3); Position.WriteBody(w);
        w.WriteHeader(3); Rotation.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        r.SkipHeader(); Position.ReadBody(r);
        r.SkipHeader(); Rotation.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntitySync Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntitySync m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40102 EntityEnterViewNotify -> Battle（服务器目标 Battle）。</summary>
struct EntityEnterViewNotify
{
    static constexpr int32_t MsgId = 40102;
    static constexpr const char* Target = "Battle";
    std::vector<EntityState> Entities;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteCount((int32_t)Entities.size()); for (const auto& it : Entities) { w.WriteHeader(8); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        int32_t _n = r.ReadCount(); Entities.clear(); Entities.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { EntityState _e; r.SkipHeader(); _e.ReadBody(r); Entities.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntityEnterViewNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntityEnterViewNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40103 EntityLeaveViewNotify -> Battle（服务器目标 Battle）。</summary>
struct EntityLeaveViewNotify
{
    static constexpr int32_t MsgId = 40103;
    static constexpr const char* Target = "Battle";
    std::vector<int64_t> EntityIds;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteCount((int32_t)EntityIds.size()); for (const auto& it : EntityIds) { w.WriteI64(it); }
    }

    void ReadBody(mp::Reader& r)
    {
        int32_t _n = r.ReadCount(); EntityIds.clear(); EntityIds.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { int64_t _e = r.ReadI64(); EntityIds.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntityLeaveViewNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntityLeaveViewNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40104 EntityStateUpdateNotify -> Battle（服务器目标 Battle）。</summary>
struct EntityStateUpdateNotify
{
    static constexpr int32_t MsgId = 40104;
    static constexpr const char* Target = "Battle";
    EntityState State;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteHeader(8); State.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        r.SkipHeader(); State.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntityStateUpdateNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntityStateUpdateNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40105 EntityDeltaSync -> Battle（服务器目标 Battle）。</summary>
struct EntityDeltaSync
{
    static constexpr int32_t MsgId = 40105;
    static constexpr const char* Target = "Battle";
    int64_t EntityId = 0;
    std::vector<uint8_t> Props;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(EntityId);
        w.WriteBytes(Props);
    }

    void ReadBody(mp::Reader& r)
    {
        EntityId = r.ReadI64();
        Props = r.ReadBytes();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntityDeltaSync Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntityDeltaSync m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>40106 EntitySnapshot -> Battle（服务器目标 Battle）。</summary>
struct EntitySnapshot
{
    static constexpr int32_t MsgId = 40106;
    static constexpr const char* Target = "Battle";
    int64_t EntityId = 0;
    std::vector<uint8_t> Props;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(EntityId);
        w.WriteBytes(Props);
    }

    void ReadBody(mp::Reader& r)
    {
        EntityId = r.ReadI64();
        Props = r.ReadBytes();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static EntitySnapshot Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        EntitySnapshot m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30001 CenterMatch -> Center（服务器目标 Center）。</summary>
struct CenterMatch
{
    static constexpr int32_t MsgId = 30001;
    static constexpr const char* Target = "Center";
    std::string CategoryId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(CategoryId);
    }

    void ReadBody(mp::Reader& r)
    {
        CategoryId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterMatch Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterMatch m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30002 CenterMatchResult -> Center（服务器目标 Center）。</summary>
struct CenterMatchResult
{
    static constexpr int32_t MsgId = 30002;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string BattleNodeId;
    std::string SceneId;
    std::string SceneType;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(6);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterMatchResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterMatchResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30003 CenterCreateRoom -> Center（服务器目标 Center）。</summary>
struct CenterCreateRoom
{
    static constexpr int32_t MsgId = 30003;
    static constexpr const char* Target = "Center";
    std::string SceneType;
    bool IsPrivate = false;
    std::string RoomName;
    std::string Password;
    int32_t MaxPlayers = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(5);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(SceneType);
        w.WriteBool(IsPrivate);
        w.WriteString(RoomName);
        w.WriteString(Password);
        w.WriteI32(MaxPlayers);
    }

    void ReadBody(mp::Reader& r)
    {
        SceneType = r.ReadString();
        IsPrivate = r.ReadBool();
        RoomName = r.ReadString();
        Password = r.ReadString();
        MaxPlayers = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterCreateRoom Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterCreateRoom m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30004 CenterCreateRoomResult -> Center（服务器目标 Center）。</summary>
struct CenterCreateRoomResult
{
    static constexpr int32_t MsgId = 30004;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string RoomName;
    std::string BattleNodeId;
    std::string SceneId;
    bool HasPassword = false;
    int32_t MaxPlayers = 0;
    int32_t CurrentPlayers = 0;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(9);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterCreateRoomResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterCreateRoomResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30005 CenterListRooms -> Center（服务器目标 Center）。</summary>
struct CenterListRooms
{
    static constexpr int32_t MsgId = 30005;
    static constexpr const char* Target = "Center";
    std::string SceneType;
    bool IncludePrivate = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(SceneType);
        w.WriteBool(IncludePrivate);
    }

    void ReadBody(mp::Reader& r)
    {
        SceneType = r.ReadString();
        IncludePrivate = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterListRooms Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterListRooms m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30006 CenterListRoomsResult -> Center（服务器目标 Center）。</summary>
struct CenterListRoomsResult
{
    static constexpr int32_t MsgId = 30006;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;
    std::vector<RoomInfo> Rooms;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount((int32_t)Rooms.size()); for (const auto& it : Rooms) { w.WriteHeader(12); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int32_t _n = r.ReadCount(); Rooms.clear(); Rooms.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { RoomInfo _e; r.SkipHeader(); _e.ReadBody(r); Rooms.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterListRoomsResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterListRoomsResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30007 CenterJoinRoom -> Center（服务器目标 Center）。</summary>
struct CenterJoinRoom
{
    static constexpr int32_t MsgId = 30007;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Password;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Password);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Password = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterJoinRoom Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterJoinRoom m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30008 CenterJoinRoomResult -> Center（服务器目标 Center）。</summary>
struct CenterJoinRoomResult
{
    static constexpr int32_t MsgId = 30008;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string RoomName;
    std::string BattleNodeId;
    std::string SceneId;
    std::string SceneType;
    bool HasPassword = false;
    int32_t MaxPlayers = 0;
    int32_t CurrentPlayers = 0;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(10);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterJoinRoomResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterJoinRoomResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30009 CenterCloseRoom -> Center（服务器目标 Center）。</summary>
struct CenterCloseRoom
{
    static constexpr int32_t MsgId = 30009;
    static constexpr const char* Target = "Center";
    std::string RoomId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterCloseRoom Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterCloseRoom m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30010 CenterCloseRoomResult -> Center（服务器目标 Center）。</summary>
struct CenterCloseRoomResult
{
    static constexpr int32_t MsgId = 30010;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterCloseRoomResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterCloseRoomResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30011 RoomClosedNotify -> Center（服务器目标 Center）。</summary>
struct RoomClosedNotify
{
    static constexpr int32_t MsgId = 30011;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomClosedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomClosedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30012 CenterUpdateRoomSettings -> Center（服务器目标 Center）。</summary>
struct CenterUpdateRoomSettings
{
    static constexpr int32_t MsgId = 30012;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string SceneType;
    std::string RoomName;
    std::string Password;
    int32_t MaxPlayers = 0;
    bool IsPrivate = false;
    std::map<std::string, std::string> CustomRules;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(7);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
        w.WriteString(Password);
        w.WriteI32(MaxPlayers);
        w.WriteBool(IsPrivate);
        w.WriteCount((int32_t)CustomRules.size()); for (const auto& kv : CustomRules) { w.WriteString(kv.first); w.WriteString(kv.second); }
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
        Password = r.ReadString();
        MaxPlayers = r.ReadI32();
        IsPrivate = r.ReadBool();
        int32_t _n = r.ReadCount(); CustomRules.clear(); for (int32_t _i = 0; _i < _n; ++_i) { std::string _k = r.ReadString(); std::string _v = r.ReadString(); CustomRules[_k] = _v; }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterUpdateRoomSettings Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterUpdateRoomSettings m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30013 CenterUpdateRoomSettingsResult -> Center（服务器目标 Center）。</summary>
struct CenterUpdateRoomSettingsResult
{
    static constexpr int32_t MsgId = 30013;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterUpdateRoomSettingsResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterUpdateRoomSettingsResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30014 RoomSettingsChangedNotify -> Center（服务器目标 Center）。</summary>
struct RoomSettingsChangedNotify
{
    static constexpr int32_t MsgId = 30014;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomSettingsChangedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomSettingsChangedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30015 CenterStartRoomGame -> Center（服务器目标 Center）。</summary>
struct CenterStartRoomGame
{
    static constexpr int32_t MsgId = 30015;
    static constexpr const char* Target = "Center";
    std::string RoomId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterStartRoomGame Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterStartRoomGame m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30016 CenterStartRoomGameResult -> Center（服务器目标 Center）。</summary>
struct CenterStartRoomGameResult
{
    static constexpr int32_t MsgId = 30016;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string BattleNodeId;
    std::string SceneId;
    std::string SceneType;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(6);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterStartRoomGameResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterStartRoomGameResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30017 RoomGameStartedNotify -> Center（服务器目标 Center）。</summary>
struct RoomGameStartedNotify
{
    static constexpr int32_t MsgId = 30017;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomGameStartedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomGameStartedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30018 RoomMemberList -> Center（服务器目标 Center）。</summary>
struct RoomMemberList
{
    static constexpr int32_t MsgId = 30018;
    static constexpr const char* Target = "Center";
    std::string RoomId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomMemberList Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomMemberList m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30019 RoomMemberListResult -> Center（服务器目标 Center）。</summary>
struct RoomMemberListResult
{
    static constexpr int32_t MsgId = 30019;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;
    RoomInfo Room;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomMemberListResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomMemberListResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30020 RoomMemberListChangedNotify -> Center（服务器目标 Center）。</summary>
struct RoomMemberListChangedNotify
{
    static constexpr int32_t MsgId = 30020;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomMemberListChangedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomMemberListChangedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30021 RoomReady -> Center（服务器目标 Center）。</summary>
struct RoomReady
{
    static constexpr int32_t MsgId = 30021;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    bool IsReady = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteBool(IsReady);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        IsReady = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomReady Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomReady m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30022 RoomReadyResult -> Center（服务器目标 Center）。</summary>
struct RoomReadyResult
{
    static constexpr int32_t MsgId = 30022;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;
    RoomInfo Room;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomReadyResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomReadyResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30023 RoomReadyChangedNotify -> Center（服务器目标 Center）。</summary>
struct RoomReadyChangedNotify
{
    static constexpr int32_t MsgId = 30023;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    int32_t UserId = 0;
    bool IsReady = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteI32(UserId);
        w.WriteBool(IsReady);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        UserId = r.ReadI32();
        IsReady = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomReadyChangedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomReadyChangedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30024 RoomTransferOwner -> Center（服务器目标 Center）。</summary>
struct RoomTransferOwner
{
    static constexpr int32_t MsgId = 30024;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    int32_t TargetUserId = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteI32(TargetUserId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        TargetUserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomTransferOwner Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomTransferOwner m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30025 RoomTransferOwnerResult -> Center（服务器目标 Center）。</summary>
struct RoomTransferOwnerResult
{
    static constexpr int32_t MsgId = 30025;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;
    RoomInfo Room;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomTransferOwnerResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomTransferOwnerResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30026 RoomOwnerChangedNotify -> Center（服务器目标 Center）。</summary>
struct RoomOwnerChangedNotify
{
    static constexpr int32_t MsgId = 30026;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    int32_t NewOwnerUserId = 0;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteI32(NewOwnerUserId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        NewOwnerUserId = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomOwnerChangedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomOwnerChangedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30027 RoomKickMember -> Center（服务器目标 Center）。</summary>
struct RoomKickMember
{
    static constexpr int32_t MsgId = 30027;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    int32_t TargetUserId = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteI32(TargetUserId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        TargetUserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomKickMember Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomKickMember m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30028 RoomKickMemberResult -> Center（服务器目标 Center）。</summary>
struct RoomKickMemberResult
{
    static constexpr int32_t MsgId = 30028;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;
    RoomInfo Room;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomKickMemberResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomKickMemberResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30029 RoomKickedNotify -> Center（服务器目标 Center）。</summary>
struct RoomKickedNotify
{
    static constexpr int32_t MsgId = 30029;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RoomKickedNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RoomKickedNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30030 CenterRoomChat -> Center（服务器目标 Center）。</summary>
struct CenterRoomChat
{
    static constexpr int32_t MsgId = 30030;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    std::string Content;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteString(Content);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        Content = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterRoomChat Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterRoomChat m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30031 CenterRoomChatResult -> Center（服务器目标 Center）。</summary>
struct CenterRoomChatResult
{
    static constexpr int32_t MsgId = 30031;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterRoomChatResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterRoomChatResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30032 CenterRoomChatNotify -> Center（服务器目标 Center）。</summary>
struct CenterRoomChatNotify
{
    static constexpr int32_t MsgId = 30032;
    static constexpr const char* Target = "Center";
    std::string RoomId;
    int32_t SenderUserId = 0;
    std::string SenderNickname;
    std::string Content;
    int64_t SendTime = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(5);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
        w.WriteI32(SenderUserId);
        w.WriteString(SenderNickname);
        w.WriteString(Content);
        w.WriteI64(SendTime);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
        SenderUserId = r.ReadI32();
        SenderNickname = r.ReadString();
        Content = r.ReadString();
        SendTime = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterRoomChatNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterRoomChatNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30033 CenterLeaveRoom -> Center（服务器目标 Center）。</summary>
struct CenterLeaveRoom
{
    static constexpr int32_t MsgId = 30033;
    static constexpr const char* Target = "Center";
    std::string RoomId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(RoomId);
    }

    void ReadBody(mp::Reader& r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterLeaveRoom Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterLeaveRoom m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>30034 CenterLeaveRoomResult -> Center（服务器目标 Center）。</summary>
struct CenterLeaveRoomResult
{
    static constexpr int32_t MsgId = 30034;
    static constexpr const char* Target = "Center";
    bool Success = false;
    std::string RoomId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static CenterLeaveRoomResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        CenterLeaveRoomResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>60001 ChatSend -> Game（服务器目标 Game）。</summary>
struct ChatSend
{
    static constexpr int32_t MsgId = 60001;
    static constexpr const char* Target = "Game";
    int32_t SenderId = 0;
    std::string SenderUniqueId;
    std::string SenderName;
    int32_t ReceiverId = 0;
    std::string ReceiverUniqueId;
    int32_t Channel = 0;
    std::string RoomId;
    std::string Content;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(8);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(SenderId);
        w.WriteString(SenderUniqueId);
        w.WriteString(SenderName);
        w.WriteI32(ReceiverId);
        w.WriteString(ReceiverUniqueId);
        w.WriteI32(Channel);
        w.WriteString(RoomId);
        w.WriteString(Content);
    }

    void ReadBody(mp::Reader& r)
    {
        SenderId = r.ReadI32();
        SenderUniqueId = r.ReadString();
        SenderName = r.ReadString();
        ReceiverId = r.ReadI32();
        ReceiverUniqueId = r.ReadString();
        Channel = r.ReadI32();
        RoomId = r.ReadString();
        Content = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ChatSend Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ChatSend m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>60002 ChatSendResult -> Game（服务器目标 Game）。</summary>
struct ChatSendResult
{
    static constexpr int32_t MsgId = 60002;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ChatSendResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ChatSendResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>60003 ChatNotify -> Game（服务器目标 Game）。</summary>
struct ChatNotify
{
    static constexpr int32_t MsgId = 60003;
    static constexpr const char* Target = "Game";
    int64_t MessageId = 0;
    int32_t SenderId = 0;
    std::string SenderUniqueId;
    std::string SenderName;
    int32_t ReceiverId = 0;
    std::string ReceiverUniqueId;
    int32_t Channel = 0;
    std::string Content;
    int64_t SendTime = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(9);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(MessageId);
        w.WriteI32(SenderId);
        w.WriteString(SenderUniqueId);
        w.WriteString(SenderName);
        w.WriteI32(ReceiverId);
        w.WriteString(ReceiverUniqueId);
        w.WriteI32(Channel);
        w.WriteString(Content);
        w.WriteI64(SendTime);
    }

    void ReadBody(mp::Reader& r)
    {
        MessageId = r.ReadI64();
        SenderId = r.ReadI32();
        SenderUniqueId = r.ReadString();
        SenderName = r.ReadString();
        ReceiverId = r.ReadI32();
        ReceiverUniqueId = r.ReadString();
        Channel = r.ReadI32();
        Content = r.ReadString();
        SendTime = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ChatNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ChatNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50001 FriendAdd -> Game（服务器目标 Game）。</summary>
struct FriendAdd
{
    static constexpr int32_t MsgId = 50001;
    static constexpr const char* Target = "Game";
    std::string TargetUniqueId;
    std::string Remark;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(TargetUniqueId);
        w.WriteString(Remark);
    }

    void ReadBody(mp::Reader& r)
    {
        TargetUniqueId = r.ReadString();
        Remark = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendAdd Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendAdd m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50002 FriendAddResult -> Game（服务器目标 Game）。</summary>
struct FriendAddResult
{
    static constexpr int32_t MsgId = 50002;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendAddResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendAddResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50003 FriendRemove -> Game（服务器目标 Game）。</summary>
struct FriendRemove
{
    static constexpr int32_t MsgId = 50003;
    static constexpr const char* Target = "Game";
    std::string FriendUniqueId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(FriendUniqueId);
    }

    void ReadBody(mp::Reader& r)
    {
        FriendUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendRemove Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendRemove m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50004 FriendRemoveResult -> Game（服务器目标 Game）。</summary>
struct FriendRemoveResult
{
    static constexpr int32_t MsgId = 50004;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendRemoveResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendRemoveResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50005 FriendSetRemark -> Game（服务器目标 Game）。</summary>
struct FriendSetRemark
{
    static constexpr int32_t MsgId = 50005;
    static constexpr const char* Target = "Game";
    std::string FriendUniqueId;
    std::string Remark;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(FriendUniqueId);
        w.WriteString(Remark);
    }

    void ReadBody(mp::Reader& r)
    {
        FriendUniqueId = r.ReadString();
        Remark = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendSetRemark Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendSetRemark m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50006 FriendSetRemarkResult -> Game（服务器目标 Game）。</summary>
struct FriendSetRemarkResult
{
    static constexpr int32_t MsgId = 50006;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendSetRemarkResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendSetRemarkResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50007 FriendGetList -> Game（服务器目标 Game）。</summary>
struct FriendGetList
{
    static constexpr int32_t MsgId = 50007;
    static constexpr const char* Target = "Game";

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(0);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
    }

    void ReadBody(mp::Reader& r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendGetList Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendGetList m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50008 FriendListResult -> Game（服务器目标 Game）。</summary>
struct FriendListResult
{
    static constexpr int32_t MsgId = 50008;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;
    std::vector<FriendInfo> Friends;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount((int32_t)Friends.size()); for (const auto& it : Friends) { w.WriteHeader(5); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int32_t _n = r.ReadCount(); Friends.clear(); Friends.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { FriendInfo _e; r.SkipHeader(); _e.ReadBody(r); Friends.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendListResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendListResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50009 FriendInviteGame -> Game（服务器目标 Game）。</summary>
struct FriendInviteGame
{
    static constexpr int32_t MsgId = 50009;
    static constexpr const char* Target = "Game";
    std::string FriendUniqueId;
    std::string RoomId;
    std::string SceneType;
    std::string RoomName;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(4);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(FriendUniqueId);
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
    }

    void ReadBody(mp::Reader& r)
    {
        FriendUniqueId = r.ReadString();
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGame Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGame m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50010 FriendInviteGameResult -> Game（服务器目标 Game）。</summary>
struct FriendInviteGameResult
{
    static constexpr int32_t MsgId = 50010;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGameResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGameResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50011 FriendInviteGameNotify -> Game（服务器目标 Game）。</summary>
struct FriendInviteGameNotify
{
    static constexpr int32_t MsgId = 50011;
    static constexpr const char* Target = "Game";
    std::string InviterUniqueId;
    std::string InviterNickname;
    std::string RoomId;
    std::string SceneType;
    std::string RoomName;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(5);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(InviterUniqueId);
        w.WriteString(InviterNickname);
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
    }

    void ReadBody(mp::Reader& r)
    {
        InviterUniqueId = r.ReadString();
        InviterNickname = r.ReadString();
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGameNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGameNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50012 BlacklistAdd -> Game（服务器目标 Game）。</summary>
struct BlacklistAdd
{
    static constexpr int32_t MsgId = 50012;
    static constexpr const char* Target = "Game";
    std::string TargetUniqueId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(TargetUniqueId);
    }

    void ReadBody(mp::Reader& r)
    {
        TargetUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistAdd Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistAdd m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50013 BlacklistAddResult -> Game（服务器目标 Game）。</summary>
struct BlacklistAddResult
{
    static constexpr int32_t MsgId = 50013;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistAddResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistAddResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50014 BlacklistRemove -> Game（服务器目标 Game）。</summary>
struct BlacklistRemove
{
    static constexpr int32_t MsgId = 50014;
    static constexpr const char* Target = "Game";
    std::string TargetUniqueId;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(TargetUniqueId);
    }

    void ReadBody(mp::Reader& r)
    {
        TargetUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistRemove Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistRemove m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50015 BlacklistRemoveResult -> Game（服务器目标 Game）。</summary>
struct BlacklistRemoveResult
{
    static constexpr int32_t MsgId = 50015;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistRemoveResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistRemoveResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50016 BlacklistGetList -> Game（服务器目标 Game）。</summary>
struct BlacklistGetList
{
    static constexpr int32_t MsgId = 50016;
    static constexpr const char* Target = "Game";

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(0);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
    }

    void ReadBody(mp::Reader& r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistGetList Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistGetList m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50017 BlacklistListResult -> Game（服务器目标 Game）。</summary>
struct BlacklistListResult
{
    static constexpr int32_t MsgId = 50017;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;
    std::vector<BlacklistInfo> Blacklists;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount((int32_t)Blacklists.size()); for (const auto& it : Blacklists) { w.WriteHeader(4); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int32_t _n = r.ReadCount(); Blacklists.clear(); Blacklists.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { BlacklistInfo _e; r.SkipHeader(); _e.ReadBody(r); Blacklists.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static BlacklistListResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        BlacklistListResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50018 FriendApply -> Game（服务器目标 Game）。</summary>
struct FriendApply
{
    static constexpr int32_t MsgId = 50018;
    static constexpr const char* Target = "Game";
    std::string TargetUniqueId;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(TargetUniqueId);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        TargetUniqueId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApply Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApply m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50019 FriendApplyResult -> Game（服务器目标 Game）。</summary>
struct FriendApplyResult
{
    static constexpr int32_t MsgId = 50019;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50020 FriendApplyNotify -> Game（服务器目标 Game）。</summary>
struct FriendApplyNotify
{
    static constexpr int32_t MsgId = 50020;
    static constexpr const char* Target = "Game";
    int64_t ApplyId = 0;
    int32_t RequesterUserId = 0;
    std::string RequesterUniqueId;
    std::string RequesterNickname;
    std::string Message;
    int64_t CreateTimeUtc = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(6);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(ApplyId);
        w.WriteI32(RequesterUserId);
        w.WriteString(RequesterUniqueId);
        w.WriteString(RequesterNickname);
        w.WriteString(Message);
        w.WriteI64(CreateTimeUtc);
    }

    void ReadBody(mp::Reader& r)
    {
        ApplyId = r.ReadI64();
        RequesterUserId = r.ReadI32();
        RequesterUniqueId = r.ReadString();
        RequesterNickname = r.ReadString();
        Message = r.ReadString();
        CreateTimeUtc = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50021 FriendApplyList -> Game（服务器目标 Game）。</summary>
struct FriendApplyList
{
    static constexpr int32_t MsgId = 50021;
    static constexpr const char* Target = "Game";

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(0);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
    }

    void ReadBody(mp::Reader& r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyList Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyList m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50022 FriendApplyListResult -> Game（服务器目标 Game）。</summary>
struct FriendApplyListResult
{
    static constexpr int32_t MsgId = 50022;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;
    std::vector<FriendApplyInfo> Applies;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount((int32_t)Applies.size()); for (const auto& it : Applies) { w.WriteHeader(7); it.WriteBody(w); }
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int32_t _n = r.ReadCount(); Applies.clear(); Applies.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; ++_i) { FriendApplyInfo _e; r.SkipHeader(); _e.ReadBody(r); Applies.push_back(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyListResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyListResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50023 FriendApplyHandle -> Game（服务器目标 Game）。</summary>
struct FriendApplyHandle
{
    static constexpr int32_t MsgId = 50023;
    static constexpr const char* Target = "Game";
    int64_t ApplyId = 0;
    bool Accept = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI64(ApplyId);
        w.WriteBool(Accept);
    }

    void ReadBody(mp::Reader& r)
    {
        ApplyId = r.ReadI64();
        Accept = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyHandle Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyHandle m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50024 FriendApplyHandleResult -> Game（服务器目标 Game）。</summary>
struct FriendApplyHandleResult
{
    static constexpr int32_t MsgId = 50024;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendApplyHandleResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendApplyHandleResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50025 FriendOnlineStatusNotify -> Game（服务器目标 Game）。</summary>
struct FriendOnlineStatusNotify
{
    static constexpr int32_t MsgId = 50025;
    static constexpr const char* Target = "Game";
    int32_t UserId = 0;
    std::string UniqueId;
    bool IsOnline = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(UserId);
        w.WriteString(UniqueId);
        w.WriteBool(IsOnline);
    }

    void ReadBody(mp::Reader& r)
    {
        UserId = r.ReadI32();
        UniqueId = r.ReadString();
        IsOnline = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendOnlineStatusNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendOnlineStatusNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50026 FriendInviteGameAck -> Game（服务器目标 Game）。</summary>
struct FriendInviteGameAck
{
    static constexpr int32_t MsgId = 50026;
    static constexpr const char* Target = "Game";
    std::string InviterUniqueId;
    std::string RoomId;
    bool Accept = false;
    std::string Reason;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(4);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(InviterUniqueId);
        w.WriteString(RoomId);
        w.WriteBool(Accept);
        w.WriteString(Reason);
    }

    void ReadBody(mp::Reader& r)
    {
        InviterUniqueId = r.ReadString();
        RoomId = r.ReadString();
        Accept = r.ReadBool();
        Reason = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGameAck Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGameAck m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50027 FriendInviteGameAckResult -> Game（服务器目标 Game）。</summary>
struct FriendInviteGameAckResult
{
    static constexpr int32_t MsgId = 50027;
    static constexpr const char* Target = "Game";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGameAckResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGameAckResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>50028 FriendInviteGameAckNotify -> Game（服务器目标 Game）。</summary>
struct FriendInviteGameAckNotify
{
    static constexpr int32_t MsgId = 50028;
    static constexpr const char* Target = "Game";
    std::string InviteeUniqueId;
    std::string InviteeNickname;
    std::string RoomId;
    bool Accept = false;
    std::string Reason;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(5);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(InviteeUniqueId);
        w.WriteString(InviteeNickname);
        w.WriteString(RoomId);
        w.WriteBool(Accept);
        w.WriteString(Reason);
    }

    void ReadBody(mp::Reader& r)
    {
        InviteeUniqueId = r.ReadString();
        InviteeNickname = r.ReadString();
        RoomId = r.ReadString();
        Accept = r.ReadBool();
        Reason = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FriendInviteGameAckNotify Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FriendInviteGameAckNotify m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10001 Login -> Login（服务器目标 Login）。</summary>
struct Login
{
    static constexpr int32_t MsgId = 10001;
    static constexpr const char* Target = "Login";
    std::string Account;
    std::string Password;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(Account);
        w.WriteString(Password);
    }

    void ReadBody(mp::Reader& r)
    {
        Account = r.ReadString();
        Password = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static Login Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        Login m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10002 LoginResult -> Login（服务器目标 Login）。</summary>
struct LoginResult
{
    static constexpr int32_t MsgId = 10002;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;
    int32_t UserId = 0;
    std::string Token;
    std::string UniqueId;
    std::string Nickname;
    std::string Email;
    int64_t LastLoginTime = 0;
    int32_t LoginCount = 0;
    bool IsAdmin = false;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(10);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteI32(UserId);
        w.WriteString(Token);
        w.WriteString(UniqueId);
        w.WriteString(Nickname);
        w.WriteString(Email);
        w.WriteI64(LastLoginTime);
        w.WriteI32(LoginCount);
        w.WriteBool(IsAdmin);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        UserId = r.ReadI32();
        Token = r.ReadString();
        UniqueId = r.ReadString();
        Nickname = r.ReadString();
        Email = r.ReadString();
        LastLoginTime = r.ReadI64();
        LoginCount = r.ReadI32();
        IsAdmin = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static LoginResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        LoginResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10003 Register -> Login（服务器目标 Login）。</summary>
struct Register
{
    static constexpr int32_t MsgId = 10003;
    static constexpr const char* Target = "Login";
    std::string Account;
    std::string Password;
    std::string Nickname;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(Account);
        w.WriteString(Password);
        w.WriteString(Nickname);
    }

    void ReadBody(mp::Reader& r)
    {
        Account = r.ReadString();
        Password = r.ReadString();
        Nickname = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static Register Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        Register m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10004 RegisterResult -> Login（服务器目标 Login）。</summary>
struct RegisterResult
{
    static constexpr int32_t MsgId = 10004;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static RegisterResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        RegisterResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10005 Logout -> Login（服务器目标 Login）。</summary>
struct Logout
{
    static constexpr int32_t MsgId = 10005;
    static constexpr const char* Target = "Login";
    int32_t UserId = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(1);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(UserId);
    }

    void ReadBody(mp::Reader& r)
    {
        UserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static Logout Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        Logout m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10006 LogoutResult -> Login（服务器目标 Login）。</summary>
struct LogoutResult
{
    static constexpr int32_t MsgId = 10006;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static LogoutResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        LogoutResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10007 ResetPassword -> Login（服务器目标 Login）。</summary>
struct ResetPassword
{
    static constexpr int32_t MsgId = 10007;
    static constexpr const char* Target = "Login";
    std::string Account;
    std::string OldPassword;
    std::string NewPassword;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(3);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(Account);
        w.WriteString(OldPassword);
        w.WriteString(NewPassword);
    }

    void ReadBody(mp::Reader& r)
    {
        Account = r.ReadString();
        OldPassword = r.ReadString();
        NewPassword = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ResetPassword Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ResetPassword m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10008 ResetPasswordResult -> Login（服务器目标 Login）。</summary>
struct ResetPasswordResult
{
    static constexpr int32_t MsgId = 10008;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static ResetPasswordResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        ResetPasswordResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10009 UpdateNickname -> Login（服务器目标 Login）。</summary>
struct UpdateNickname
{
    static constexpr int32_t MsgId = 10009;
    static constexpr const char* Target = "Login";
    int32_t UserId = 0;
    std::string NewNickname;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteI32(UserId);
        w.WriteString(NewNickname);
    }

    void ReadBody(mp::Reader& r)
    {
        UserId = r.ReadI32();
        NewNickname = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static UpdateNickname Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        UpdateNickname m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10010 UpdateNicknameResult -> Login（服务器目标 Login）。</summary>
struct UpdateNicknameResult
{
    static constexpr int32_t MsgId = 10010;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static UpdateNicknameResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        UpdateNicknameResult m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10011 KickedOff -> Login（服务器目标 Login）。</summary>
struct KickedOff
{
    static constexpr int32_t MsgId = 10011;
    static constexpr const char* Target = "Login";
    std::string Reason;
    int64_t Time = 0;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(Reason);
        w.WriteI64(Time);
    }

    void ReadBody(mp::Reader& r)
    {
        Reason = r.ReadString();
        Time = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static KickedOff Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        KickedOff m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10012 FindPasswordWithCode -> Login（服务器目标 Login）。</summary>
struct FindPasswordWithCode
{
    static constexpr int32_t MsgId = 10012;
    static constexpr const char* Target = "Login";
    std::string Account;
    std::string Email;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteString(Account);
        w.WriteString(Email);
    }

    void ReadBody(mp::Reader& r)
    {
        Account = r.ReadString();
        Email = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FindPasswordWithCode Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FindPasswordWithCode m;
        m.ReadBody(r);
        return m;
    }
};

/// <summary>10013 FindPasswordWithCodeResult -> Login（服务器目标 Login）。</summary>
struct FindPasswordWithCodeResult
{
    static constexpr int32_t MsgId = 10013;
    static constexpr const char* Target = "Login";
    bool Success = false;
    std::string Message;

    /// <summary>序列化为完整负载（对象头 + 成员数据），不含帧头。</summary>
    std::vector<uint8_t> Serialize() const
    {
        mp::Writer w;
        w.WriteHeader(2);
        WriteBody(w);
        return w.Take();
    }

    void WriteBody(mp::Writer& w) const
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    void ReadBody(mp::Reader& r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头后读成员数据）。</summary>
    static FindPasswordWithCodeResult Deserialize(const uint8_t* data, size_t size)
    {
        mp::Reader r(data, size);
        r.SkipHeader();
        FindPasswordWithCodeResult m;
        m.ReadBody(r);
        return m;
    }
};

    /// <summary>消息 ID 常量。</summary>
    inline constexpr int32_t BattleJoinMsgId = 40001;
    inline constexpr int32_t BattleJoinResultMsgId = 40002;
    inline constexpr int32_t BattleFrameSyncMsgId = 40003;
    inline constexpr int32_t BattleLeaveRoomMsgId = 40004;
    inline constexpr int32_t BattleLeaveRoomResultMsgId = 40005;
    inline constexpr int32_t ScriptActionMsgId = 40006;
    inline constexpr int32_t ClientTimeSyncMsgId = 40010;
    inline constexpr int32_t ServerTimeSyncMsgId = 40011;
    inline constexpr int32_t EntitySyncMsgId = 40101;
    inline constexpr int32_t EntityEnterViewNotifyMsgId = 40102;
    inline constexpr int32_t EntityLeaveViewNotifyMsgId = 40103;
    inline constexpr int32_t EntityStateUpdateNotifyMsgId = 40104;
    inline constexpr int32_t EntityDeltaSyncMsgId = 40105;
    inline constexpr int32_t EntitySnapshotMsgId = 40106;
    inline constexpr int32_t CenterMatchMsgId = 30001;
    inline constexpr int32_t CenterMatchResultMsgId = 30002;
    inline constexpr int32_t CenterCreateRoomMsgId = 30003;
    inline constexpr int32_t CenterCreateRoomResultMsgId = 30004;
    inline constexpr int32_t CenterListRoomsMsgId = 30005;
    inline constexpr int32_t CenterListRoomsResultMsgId = 30006;
    inline constexpr int32_t CenterJoinRoomMsgId = 30007;
    inline constexpr int32_t CenterJoinRoomResultMsgId = 30008;
    inline constexpr int32_t CenterCloseRoomMsgId = 30009;
    inline constexpr int32_t CenterCloseRoomResultMsgId = 30010;
    inline constexpr int32_t RoomClosedNotifyMsgId = 30011;
    inline constexpr int32_t CenterUpdateRoomSettingsMsgId = 30012;
    inline constexpr int32_t CenterUpdateRoomSettingsResultMsgId = 30013;
    inline constexpr int32_t RoomSettingsChangedNotifyMsgId = 30014;
    inline constexpr int32_t CenterStartRoomGameMsgId = 30015;
    inline constexpr int32_t CenterStartRoomGameResultMsgId = 30016;
    inline constexpr int32_t RoomGameStartedNotifyMsgId = 30017;
    inline constexpr int32_t RoomMemberListMsgId = 30018;
    inline constexpr int32_t RoomMemberListResultMsgId = 30019;
    inline constexpr int32_t RoomMemberListChangedNotifyMsgId = 30020;
    inline constexpr int32_t RoomReadyMsgId = 30021;
    inline constexpr int32_t RoomReadyResultMsgId = 30022;
    inline constexpr int32_t RoomReadyChangedNotifyMsgId = 30023;
    inline constexpr int32_t RoomTransferOwnerMsgId = 30024;
    inline constexpr int32_t RoomTransferOwnerResultMsgId = 30025;
    inline constexpr int32_t RoomOwnerChangedNotifyMsgId = 30026;
    inline constexpr int32_t RoomKickMemberMsgId = 30027;
    inline constexpr int32_t RoomKickMemberResultMsgId = 30028;
    inline constexpr int32_t RoomKickedNotifyMsgId = 30029;
    inline constexpr int32_t CenterRoomChatMsgId = 30030;
    inline constexpr int32_t CenterRoomChatResultMsgId = 30031;
    inline constexpr int32_t CenterRoomChatNotifyMsgId = 30032;
    inline constexpr int32_t CenterLeaveRoomMsgId = 30033;
    inline constexpr int32_t CenterLeaveRoomResultMsgId = 30034;
    inline constexpr int32_t ChatSendMsgId = 60001;
    inline constexpr int32_t ChatSendResultMsgId = 60002;
    inline constexpr int32_t ChatNotifyMsgId = 60003;
    inline constexpr int32_t FriendAddMsgId = 50001;
    inline constexpr int32_t FriendAddResultMsgId = 50002;
    inline constexpr int32_t FriendRemoveMsgId = 50003;
    inline constexpr int32_t FriendRemoveResultMsgId = 50004;
    inline constexpr int32_t FriendSetRemarkMsgId = 50005;
    inline constexpr int32_t FriendSetRemarkResultMsgId = 50006;
    inline constexpr int32_t FriendGetListMsgId = 50007;
    inline constexpr int32_t FriendListResultMsgId = 50008;
    inline constexpr int32_t FriendInviteGameMsgId = 50009;
    inline constexpr int32_t FriendInviteGameResultMsgId = 50010;
    inline constexpr int32_t FriendInviteGameNotifyMsgId = 50011;
    inline constexpr int32_t BlacklistAddMsgId = 50012;
    inline constexpr int32_t BlacklistAddResultMsgId = 50013;
    inline constexpr int32_t BlacklistRemoveMsgId = 50014;
    inline constexpr int32_t BlacklistRemoveResultMsgId = 50015;
    inline constexpr int32_t BlacklistGetListMsgId = 50016;
    inline constexpr int32_t BlacklistListResultMsgId = 50017;
    inline constexpr int32_t FriendApplyMsgId = 50018;
    inline constexpr int32_t FriendApplyResultMsgId = 50019;
    inline constexpr int32_t FriendApplyNotifyMsgId = 50020;
    inline constexpr int32_t FriendApplyListMsgId = 50021;
    inline constexpr int32_t FriendApplyListResultMsgId = 50022;
    inline constexpr int32_t FriendApplyHandleMsgId = 50023;
    inline constexpr int32_t FriendApplyHandleResultMsgId = 50024;
    inline constexpr int32_t FriendOnlineStatusNotifyMsgId = 50025;
    inline constexpr int32_t FriendInviteGameAckMsgId = 50026;
    inline constexpr int32_t FriendInviteGameAckResultMsgId = 50027;
    inline constexpr int32_t FriendInviteGameAckNotifyMsgId = 50028;
    inline constexpr int32_t LoginMsgId = 10001;
    inline constexpr int32_t LoginResultMsgId = 10002;
    inline constexpr int32_t RegisterMsgId = 10003;
    inline constexpr int32_t RegisterResultMsgId = 10004;
    inline constexpr int32_t LogoutMsgId = 10005;
    inline constexpr int32_t LogoutResultMsgId = 10006;
    inline constexpr int32_t ResetPasswordMsgId = 10007;
    inline constexpr int32_t ResetPasswordResultMsgId = 10008;
    inline constexpr int32_t UpdateNicknameMsgId = 10009;
    inline constexpr int32_t UpdateNicknameResultMsgId = 10010;
    inline constexpr int32_t KickedOffMsgId = 10011;
    inline constexpr int32_t FindPasswordWithCodeMsgId = 10012;
    inline constexpr int32_t FindPasswordWithCodeResultMsgId = 10013;
}
