// <auto-generated> 由 ClientGen 生成，请勿手动修改。
#nullable enable
using System;
using System.Collections.Generic;

namespace ClientProtocol;

/// <summary>结构体 Vector3（3 个成员）。</summary>
public sealed class Vector3
{
    public float X = 0f;
    public float Y = 0f;
    public float Z = 0f;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteFloat(X);
        w.WriteFloat(Y);
        w.WriteFloat(Z);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        X = r.ReadFloat();
        Y = r.ReadFloat();
        Z = r.ReadFloat();
    }
}

/// <summary>结构体 EntityState（8 个成员）。</summary>
public sealed class EntityState
{
    public long EntityId = 0L;
    public string Nickname = "";
    public Vector3 Position = new Vector3();
    public Vector3 Rotation = new Vector3();
    public int Hp = 0;
    public int MaxHp = 0;
    public int Score = 0;
    public List<int> Equipment = new List<int>();

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(EntityId);
        w.WriteString(Nickname);
        w.WriteHeader(3); Position.WriteBody(w);
        w.WriteHeader(3); Rotation.WriteBody(w);
        w.WriteI32(Hp);
        w.WriteI32(MaxHp);
        w.WriteI32(Score);
        w.WriteCount(Equipment.Count); foreach (var it in Equipment) { w.WriteI32(it); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        EntityId = r.ReadI64();
        Nickname = r.ReadString();
        r.SkipHeader(); Position.ReadBody(r);
        r.SkipHeader(); Rotation.ReadBody(r);
        Hp = r.ReadI32();
        MaxHp = r.ReadI32();
        Score = r.ReadI32();
        int _n = r.ReadCount(); Equipment = new List<int>(); for (int _i = 0; _i < _n; _i++) { var _e = r.ReadI32(); Equipment.Add(_e); }
    }
}

/// <summary>结构体 PlayerInput（4 个成员）。</summary>
public sealed class PlayerInput
{
    public long InputId = 0L;
    public int Buttons = 0;
    public float MoveX = 0f;
    public float MoveY = 0f;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(InputId);
        w.WriteI32(Buttons);
        w.WriteFloat(MoveX);
        w.WriteFloat(MoveY);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        InputId = r.ReadI64();
        Buttons = r.ReadI32();
        MoveX = r.ReadFloat();
        MoveY = r.ReadFloat();
    }
}

/// <summary>结构体 RoomInfo（12 个成员）。</summary>
public sealed class RoomInfo
{
    public string RoomId = "";
    public string RoomName = "";
    public string SceneId = "";
    public string SceneType = "";
    public string BattleNodeId = "";
    public int OwnerUserId = 0;
    public bool IsPrivate = false;
    public bool HasPassword = false;
    public int MaxPlayers = 0;
    public int CurrentPlayers = 0;
    public string RoomStatus = "";
    public long CreatedAtUtc = 0L;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(BattleNodeId);
        w.WriteI32(OwnerUserId);
        w.WriteBool(IsPrivate);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(RoomStatus);
        w.WriteI64(CreatedAtUtc);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        BattleNodeId = r.ReadString();
        OwnerUserId = r.ReadI32();
        IsPrivate = r.ReadBool();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        RoomStatus = r.ReadString();
        CreatedAtUtc = r.ReadI64();
    }
}

/// <summary>结构体 FriendInfo（5 个成员）。</summary>
public sealed class FriendInfo
{
    public int FriendUserId = 0;
    public string FriendUniqueId = "";
    public string Nickname = "";
    public string Remark = "";
    public bool IsOnline = false;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(FriendUserId);
        w.WriteString(FriendUniqueId);
        w.WriteString(Nickname);
        w.WriteString(Remark);
        w.WriteBool(IsOnline);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        FriendUserId = r.ReadI32();
        FriendUniqueId = r.ReadString();
        Nickname = r.ReadString();
        Remark = r.ReadString();
        IsOnline = r.ReadBool();
    }
}

/// <summary>结构体 BlacklistInfo（4 个成员）。</summary>
public sealed class BlacklistInfo
{
    public int BlockedUserId = 0;
    public string BlockedUniqueId = "";
    public string BlockedNickname = "";
    public long AddTime = 0L;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(BlockedUserId);
        w.WriteString(BlockedUniqueId);
        w.WriteString(BlockedNickname);
        w.WriteI64(AddTime);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        BlockedUserId = r.ReadI32();
        BlockedUniqueId = r.ReadString();
        BlockedNickname = r.ReadString();
        AddTime = r.ReadI64();
    }
}

/// <summary>结构体 FriendApplyInfo（7 个成员）。</summary>
public sealed class FriendApplyInfo
{
    public long ApplyId = 0L;
    public int RequesterUserId = 0;
    public string RequesterUniqueId = "";
    public string RequesterNickname = "";
    public string Message = "";
    public string Status = "";
    public long CreateTimeUtc = 0L;

    /// <summary>写入成员数据（不含对象头字节；直接结构体字段会写入自身对象头）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(ApplyId);
        w.WriteI32(RequesterUserId);
        w.WriteString(RequesterUniqueId);
        w.WriteString(RequesterNickname);
        w.WriteString(Message);
        w.WriteString(Status);
        w.WriteI64(CreateTimeUtc);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        ApplyId = r.ReadI64();
        RequesterUserId = r.ReadI32();
        RequesterUniqueId = r.ReadString();
        RequesterNickname = r.ReadString();
        Message = r.ReadString();
        Status = r.ReadString();
        CreateTimeUtc = r.ReadI64();
    }
}

/// <summary>40001 BattleJoin -> Battle（服务器目标 Battle）。</summary>
public sealed class BattleJoin : IMessage
{
    public const int MsgId = 40001;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string SceneName = "";
    public string SceneType = "";
    public int MaxPlayers = 0;
    public Dictionary<string, string> CustomRules = new Dictionary<string, string>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(5); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(SceneName);
        w.WriteString(SceneType);
        w.WriteI32(MaxPlayers);
        w.WriteCount(CustomRules.Count); foreach (var kv in CustomRules) { w.WriteString(kv.Key); w.WriteString(kv.Value); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        SceneName = r.ReadString();
        SceneType = r.ReadString();
        MaxPlayers = r.ReadI32();
        int _n = r.ReadCount(); CustomRules = new Dictionary<string, string>(); for (int _i = 0; _i < _n; _i++) { var _k = r.ReadString(); var _v = r.ReadString(); CustomRules[_k] = _v; }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BattleJoin Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BattleJoin(); m.ReadBody(r); return m;
    }
}

/// <summary>40002 BattleJoinResult -> Battle（服务器目标 Battle）。</summary>
public sealed class BattleJoinResult : IMessage
{
    public const int MsgId = 40002;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BattleJoinResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BattleJoinResult(); m.ReadBody(r); return m;
    }
}

/// <summary>40003 BattleFrameSync -> Battle（服务器目标 Battle）。</summary>
public sealed class BattleFrameSync : IMessage
{
    public const int MsgId = 40003;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public int FrameId = 0;
    public List<PlayerInput> Inputs = new List<PlayerInput>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(FrameId);
        w.WriteCount(Inputs.Count); foreach (var it in Inputs) { w.WriteHeader(4); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        FrameId = r.ReadI32();
        int _n = r.ReadCount(); Inputs = new List<PlayerInput>(); for (int _i = 0; _i < _n; _i++) { var _e = new PlayerInput(); r.SkipHeader(); _e.ReadBody(r); Inputs.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BattleFrameSync Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BattleFrameSync(); m.ReadBody(r); return m;
    }
}

/// <summary>40004 BattleLeaveRoom -> Battle（服务器目标 Battle）。</summary>
public sealed class BattleLeaveRoom : IMessage
{
    public const int MsgId = 40004;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BattleLeaveRoom Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BattleLeaveRoom(); m.ReadBody(r); return m;
    }
}

/// <summary>40005 BattleLeaveRoomResult -> Battle（服务器目标 Battle）。</summary>
public sealed class BattleLeaveRoomResult : IMessage
{
    public const int MsgId = 40005;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BattleLeaveRoomResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BattleLeaveRoomResult(); m.ReadBody(r); return m;
    }
}

/// <summary>40006 ScriptAction -> Battle（服务器目标 Battle）。</summary>
public sealed class ScriptAction : IMessage
{
    public const int MsgId = 40006;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public long EntityId = 0L;
    public string Method = "";
    public List<int> Args = new List<int>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(EntityId);
        w.WriteString(Method);
        w.WriteCount(Args.Count); foreach (var it in Args) { w.WriteI32(it); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        EntityId = r.ReadI64();
        Method = r.ReadString();
        int _n = r.ReadCount(); Args = new List<int>(); for (int _i = 0; _i < _n; _i++) { var _e = r.ReadI32(); Args.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ScriptAction Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ScriptAction(); m.ReadBody(r); return m;
    }
}

/// <summary>40010 ClientTimeSync -> Battle（服务器目标 Battle）。</summary>
public sealed class ClientTimeSync : IMessage
{
    public const int MsgId = 40010;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public long ClientSendMs = 0L;
    public long LastServerSendMs = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(ClientSendMs);
        w.WriteI64(LastServerSendMs);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        ClientSendMs = r.ReadI64();
        LastServerSendMs = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ClientTimeSync Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ClientTimeSync(); m.ReadBody(r); return m;
    }
}

/// <summary>40011 ServerTimeSync -> Battle（服务器目标 Battle）。</summary>
public sealed class ServerTimeSync : IMessage
{
    public const int MsgId = 40011;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public long ClientSendMs = 0L;
    public long ServerRecvMs = 0L;
    public long ServerSendMs = 0L;
    public long AuthFrame = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(4); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(ClientSendMs);
        w.WriteI64(ServerRecvMs);
        w.WriteI64(ServerSendMs);
        w.WriteI64(AuthFrame);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        ClientSendMs = r.ReadI64();
        ServerRecvMs = r.ReadI64();
        ServerSendMs = r.ReadI64();
        AuthFrame = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ServerTimeSync Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ServerTimeSync(); m.ReadBody(r); return m;
    }
}

/// <summary>40101 EntitySync -> Battle（服务器目标 Battle）。</summary>
public sealed class EntitySync : IMessage
{
    public const int MsgId = 40101;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public Vector3 Position = new Vector3();
    public Vector3 Rotation = new Vector3();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteHeader(3); Position.WriteBody(w);
        w.WriteHeader(3); Rotation.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        r.SkipHeader(); Position.ReadBody(r);
        r.SkipHeader(); Rotation.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntitySync Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntitySync(); m.ReadBody(r); return m;
    }
}

/// <summary>40102 EntityEnterViewNotify -> Battle（服务器目标 Battle）。</summary>
public sealed class EntityEnterViewNotify : IMessage
{
    public const int MsgId = 40102;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public List<EntityState> Entities = new List<EntityState>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteCount(Entities.Count); foreach (var it in Entities) { w.WriteHeader(8); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        int _n = r.ReadCount(); Entities = new List<EntityState>(); for (int _i = 0; _i < _n; _i++) { var _e = new EntityState(); r.SkipHeader(); _e.ReadBody(r); Entities.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntityEnterViewNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntityEnterViewNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>40103 EntityLeaveViewNotify -> Battle（服务器目标 Battle）。</summary>
public sealed class EntityLeaveViewNotify : IMessage
{
    public const int MsgId = 40103;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public List<long> EntityIds = new List<long>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteCount(EntityIds.Count); foreach (var it in EntityIds) { w.WriteI64(it); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        int _n = r.ReadCount(); EntityIds = new List<long>(); for (int _i = 0; _i < _n; _i++) { var _e = r.ReadI64(); EntityIds.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntityLeaveViewNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntityLeaveViewNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>40104 EntityStateUpdateNotify -> Battle（服务器目标 Battle）。</summary>
public sealed class EntityStateUpdateNotify : IMessage
{
    public const int MsgId = 40104;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public EntityState State = new EntityState();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteHeader(8); State.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        r.SkipHeader(); State.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntityStateUpdateNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntityStateUpdateNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>40105 EntityDeltaSync -> Battle（服务器目标 Battle）。</summary>
public sealed class EntityDeltaSync : IMessage
{
    public const int MsgId = 40105;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public long EntityId = 0L;
    public byte[] Props = Array.Empty<byte>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(EntityId);
        w.WriteBytes(Props);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        EntityId = r.ReadI64();
        Props = r.ReadBytes();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntityDeltaSync Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntityDeltaSync(); m.ReadBody(r); return m;
    }
}

/// <summary>40106 EntitySnapshot -> Battle（服务器目标 Battle）。</summary>
public sealed class EntitySnapshot : IMessage
{
    public const int MsgId = 40106;
    public const string Target = "Battle";
    int IMessage.MessageId => MsgId;
    public long EntityId = 0L;
    public byte[] Props = Array.Empty<byte>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(EntityId);
        w.WriteBytes(Props);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        EntityId = r.ReadI64();
        Props = r.ReadBytes();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static EntitySnapshot Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new EntitySnapshot(); m.ReadBody(r); return m;
    }
}

/// <summary>30001 CenterMatch -> Center（服务器目标 Center）。</summary>
public sealed class CenterMatch : IMessage
{
    public const int MsgId = 30001;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string CategoryId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(CategoryId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        CategoryId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterMatch Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterMatch(); m.ReadBody(r); return m;
    }
}

/// <summary>30002 CenterMatchResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterMatchResult : IMessage
{
    public const int MsgId = 30002;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string BattleNodeId = "";
    public string SceneId = "";
    public string SceneType = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(6); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterMatchResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterMatchResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30003 CenterCreateRoom -> Center（服务器目标 Center）。</summary>
public sealed class CenterCreateRoom : IMessage
{
    public const int MsgId = 30003;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string SceneType = "";
    public bool IsPrivate = false;
    public string RoomName = "";
    public string Password = "";
    public int MaxPlayers = 0;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(5); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(SceneType);
        w.WriteBool(IsPrivate);
        w.WriteString(RoomName);
        w.WriteString(Password);
        w.WriteI32(MaxPlayers);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        SceneType = r.ReadString();
        IsPrivate = r.ReadBool();
        RoomName = r.ReadString();
        Password = r.ReadString();
        MaxPlayers = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterCreateRoom Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterCreateRoom(); m.ReadBody(r); return m;
    }
}

/// <summary>30004 CenterCreateRoomResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterCreateRoomResult : IMessage
{
    public const int MsgId = 30004;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string RoomName = "";
    public string BattleNodeId = "";
    public string SceneId = "";
    public bool HasPassword = false;
    public int MaxPlayers = 0;
    public int CurrentPlayers = 0;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(9); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterCreateRoomResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterCreateRoomResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30005 CenterListRooms -> Center（服务器目标 Center）。</summary>
public sealed class CenterListRooms : IMessage
{
    public const int MsgId = 30005;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string SceneType = "";
    public bool IncludePrivate = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(SceneType);
        w.WriteBool(IncludePrivate);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        SceneType = r.ReadString();
        IncludePrivate = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterListRooms Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterListRooms(); m.ReadBody(r); return m;
    }
}

/// <summary>30006 CenterListRoomsResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterListRoomsResult : IMessage
{
    public const int MsgId = 30006;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public List<RoomInfo> Rooms = new List<RoomInfo>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount(Rooms.Count); foreach (var it in Rooms) { w.WriteHeader(12); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int _n = r.ReadCount(); Rooms = new List<RoomInfo>(); for (int _i = 0; _i < _n; _i++) { var _e = new RoomInfo(); r.SkipHeader(); _e.ReadBody(r); Rooms.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterListRoomsResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterListRoomsResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30007 CenterJoinRoom -> Center（服务器目标 Center）。</summary>
public sealed class CenterJoinRoom : IMessage
{
    public const int MsgId = 30007;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Password = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Password);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Password = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterJoinRoom Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterJoinRoom(); m.ReadBody(r); return m;
    }
}

/// <summary>30008 CenterJoinRoomResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterJoinRoomResult : IMessage
{
    public const int MsgId = 30008;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string RoomName = "";
    public string BattleNodeId = "";
    public string SceneId = "";
    public string SceneType = "";
    public bool HasPassword = false;
    public int MaxPlayers = 0;
    public int CurrentPlayers = 0;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(10); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(RoomName);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteBool(HasPassword);
        w.WriteI32(MaxPlayers);
        w.WriteI32(CurrentPlayers);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        RoomName = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        HasPassword = r.ReadBool();
        MaxPlayers = r.ReadI32();
        CurrentPlayers = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterJoinRoomResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterJoinRoomResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30009 CenterCloseRoom -> Center（服务器目标 Center）。</summary>
public sealed class CenterCloseRoom : IMessage
{
    public const int MsgId = 30009;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterCloseRoom Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterCloseRoom(); m.ReadBody(r); return m;
    }
}

/// <summary>30010 CenterCloseRoomResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterCloseRoomResult : IMessage
{
    public const int MsgId = 30010;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterCloseRoomResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterCloseRoomResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30011 RoomClosedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomClosedNotify : IMessage
{
    public const int MsgId = 30011;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomClosedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomClosedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30012 CenterUpdateRoomSettings -> Center（服务器目标 Center）。</summary>
public sealed class CenterUpdateRoomSettings : IMessage
{
    public const int MsgId = 30012;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string SceneType = "";
    public string RoomName = "";
    public string Password = "";
    public int MaxPlayers = 0;
    public bool IsPrivate = false;
    public Dictionary<string, string> CustomRules = new Dictionary<string, string>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(7); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
        w.WriteString(Password);
        w.WriteI32(MaxPlayers);
        w.WriteBool(IsPrivate);
        w.WriteCount(CustomRules.Count); foreach (var kv in CustomRules) { w.WriteString(kv.Key); w.WriteString(kv.Value); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
        Password = r.ReadString();
        MaxPlayers = r.ReadI32();
        IsPrivate = r.ReadBool();
        int _n = r.ReadCount(); CustomRules = new Dictionary<string, string>(); for (int _i = 0; _i < _n; _i++) { var _k = r.ReadString(); var _v = r.ReadString(); CustomRules[_k] = _v; }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterUpdateRoomSettings Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterUpdateRoomSettings(); m.ReadBody(r); return m;
    }
}

/// <summary>30013 CenterUpdateRoomSettingsResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterUpdateRoomSettingsResult : IMessage
{
    public const int MsgId = 30013;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterUpdateRoomSettingsResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterUpdateRoomSettingsResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30014 RoomSettingsChangedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomSettingsChangedNotify : IMessage
{
    public const int MsgId = 30014;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomSettingsChangedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomSettingsChangedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30015 CenterStartRoomGame -> Center（服务器目标 Center）。</summary>
public sealed class CenterStartRoomGame : IMessage
{
    public const int MsgId = 30015;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterStartRoomGame Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterStartRoomGame(); m.ReadBody(r); return m;
    }
}

/// <summary>30016 CenterStartRoomGameResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterStartRoomGameResult : IMessage
{
    public const int MsgId = 30016;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string BattleNodeId = "";
    public string SceneId = "";
    public string SceneType = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(6); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(BattleNodeId);
        w.WriteString(SceneId);
        w.WriteString(SceneType);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        BattleNodeId = r.ReadString();
        SceneId = r.ReadString();
        SceneType = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterStartRoomGameResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterStartRoomGameResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30017 RoomGameStartedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomGameStartedNotify : IMessage
{
    public const int MsgId = 30017;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomGameStartedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomGameStartedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30018 RoomMemberList -> Center（服务器目标 Center）。</summary>
public sealed class RoomMemberList : IMessage
{
    public const int MsgId = 30018;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomMemberList Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomMemberList(); m.ReadBody(r); return m;
    }
}

/// <summary>30019 RoomMemberListResult -> Center（服务器目标 Center）。</summary>
public sealed class RoomMemberListResult : IMessage
{
    public const int MsgId = 30019;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public RoomInfo Room = new RoomInfo();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomMemberListResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomMemberListResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30020 RoomMemberListChangedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomMemberListChangedNotify : IMessage
{
    public const int MsgId = 30020;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomMemberListChangedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomMemberListChangedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30021 RoomReady -> Center（服务器目标 Center）。</summary>
public sealed class RoomReady : IMessage
{
    public const int MsgId = 30021;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public bool IsReady = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteBool(IsReady);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        IsReady = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomReady Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomReady(); m.ReadBody(r); return m;
    }
}

/// <summary>30022 RoomReadyResult -> Center（服务器目标 Center）。</summary>
public sealed class RoomReadyResult : IMessage
{
    public const int MsgId = 30022;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public RoomInfo Room = new RoomInfo();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomReadyResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomReadyResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30023 RoomReadyChangedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomReadyChangedNotify : IMessage
{
    public const int MsgId = 30023;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public int UserId = 0;
    public bool IsReady = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteI32(UserId);
        w.WriteBool(IsReady);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        UserId = r.ReadI32();
        IsReady = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomReadyChangedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomReadyChangedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30024 RoomTransferOwner -> Center（服务器目标 Center）。</summary>
public sealed class RoomTransferOwner : IMessage
{
    public const int MsgId = 30024;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public int TargetUserId = 0;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteI32(TargetUserId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        TargetUserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomTransferOwner Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomTransferOwner(); m.ReadBody(r); return m;
    }
}

/// <summary>30025 RoomTransferOwnerResult -> Center（服务器目标 Center）。</summary>
public sealed class RoomTransferOwnerResult : IMessage
{
    public const int MsgId = 30025;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public RoomInfo Room = new RoomInfo();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomTransferOwnerResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomTransferOwnerResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30026 RoomOwnerChangedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomOwnerChangedNotify : IMessage
{
    public const int MsgId = 30026;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public int NewOwnerUserId = 0;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteI32(NewOwnerUserId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        NewOwnerUserId = r.ReadI32();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomOwnerChangedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomOwnerChangedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30027 RoomKickMember -> Center（服务器目标 Center）。</summary>
public sealed class RoomKickMember : IMessage
{
    public const int MsgId = 30027;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public int TargetUserId = 0;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteI32(TargetUserId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        TargetUserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomKickMember Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomKickMember(); m.ReadBody(r); return m;
    }
}

/// <summary>30028 RoomKickMemberResult -> Center（服务器目标 Center）。</summary>
public sealed class RoomKickMemberResult : IMessage
{
    public const int MsgId = 30028;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public RoomInfo Room = new RoomInfo();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteHeader(12); Room.WriteBody(w);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        r.SkipHeader(); Room.ReadBody(r);
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomKickMemberResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomKickMemberResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30029 RoomKickedNotify -> Center（服务器目标 Center）。</summary>
public sealed class RoomKickedNotify : IMessage
{
    public const int MsgId = 30029;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RoomKickedNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RoomKickedNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30030 CenterRoomChat -> Center（服务器目标 Center）。</summary>
public sealed class CenterRoomChat : IMessage
{
    public const int MsgId = 30030;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public string Content = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteString(Content);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        Content = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterRoomChat Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterRoomChat(); m.ReadBody(r); return m;
    }
}

/// <summary>30031 CenterRoomChatResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterRoomChatResult : IMessage
{
    public const int MsgId = 30031;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterRoomChatResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterRoomChatResult(); m.ReadBody(r); return m;
    }
}

/// <summary>30032 CenterRoomChatNotify -> Center（服务器目标 Center）。</summary>
public sealed class CenterRoomChatNotify : IMessage
{
    public const int MsgId = 30032;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";
    public int SenderUserId = 0;
    public string SenderNickname = "";
    public string Content = "";
    public long SendTime = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(5); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
        w.WriteI32(SenderUserId);
        w.WriteString(SenderNickname);
        w.WriteString(Content);
        w.WriteI64(SendTime);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
        SenderUserId = r.ReadI32();
        SenderNickname = r.ReadString();
        Content = r.ReadString();
        SendTime = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterRoomChatNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterRoomChatNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>30033 CenterLeaveRoom -> Center（服务器目标 Center）。</summary>
public sealed class CenterLeaveRoom : IMessage
{
    public const int MsgId = 30033;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public string RoomId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(RoomId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        RoomId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterLeaveRoom Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterLeaveRoom(); m.ReadBody(r); return m;
    }
}

/// <summary>30034 CenterLeaveRoomResult -> Center（服务器目标 Center）。</summary>
public sealed class CenterLeaveRoomResult : IMessage
{
    public const int MsgId = 30034;
    public const string Target = "Center";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string RoomId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(RoomId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        RoomId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static CenterLeaveRoomResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new CenterLeaveRoomResult(); m.ReadBody(r); return m;
    }
}

/// <summary>60001 ChatSend -> Game（服务器目标 Game）。</summary>
public sealed class ChatSend : IMessage
{
    public const int MsgId = 60001;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public int SenderId = 0;
    public string SenderUniqueId = "";
    public string SenderName = "";
    public int ReceiverId = 0;
    public string ReceiverUniqueId = "";
    public int Channel = 0;
    public string RoomId = "";
    public string Content = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(8); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(SenderId);
        w.WriteString(SenderUniqueId);
        w.WriteString(SenderName);
        w.WriteI32(ReceiverId);
        w.WriteString(ReceiverUniqueId);
        w.WriteI32(Channel);
        w.WriteString(RoomId);
        w.WriteString(Content);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        SenderId = r.ReadI32();
        SenderUniqueId = r.ReadString();
        SenderName = r.ReadString();
        ReceiverId = r.ReadI32();
        ReceiverUniqueId = r.ReadString();
        Channel = r.ReadI32();
        RoomId = r.ReadString();
        Content = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ChatSend Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ChatSend(); m.ReadBody(r); return m;
    }
}

/// <summary>60002 ChatSendResult -> Game（服务器目标 Game）。</summary>
public sealed class ChatSendResult : IMessage
{
    public const int MsgId = 60002;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ChatSendResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ChatSendResult(); m.ReadBody(r); return m;
    }
}

/// <summary>60003 ChatNotify -> Game（服务器目标 Game）。</summary>
public sealed class ChatNotify : IMessage
{
    public const int MsgId = 60003;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public long MessageId = 0L;
    public int SenderId = 0;
    public string SenderUniqueId = "";
    public string SenderName = "";
    public int ReceiverId = 0;
    public string ReceiverUniqueId = "";
    public int Channel = 0;
    public string Content = "";
    public long SendTime = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(9); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(MessageId);
        w.WriteI32(SenderId);
        w.WriteString(SenderUniqueId);
        w.WriteString(SenderName);
        w.WriteI32(ReceiverId);
        w.WriteString(ReceiverUniqueId);
        w.WriteI32(Channel);
        w.WriteString(Content);
        w.WriteI64(SendTime);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        MessageId = r.ReadI64();
        SenderId = r.ReadI32();
        SenderUniqueId = r.ReadString();
        SenderName = r.ReadString();
        ReceiverId = r.ReadI32();
        ReceiverUniqueId = r.ReadString();
        Channel = r.ReadI32();
        Content = r.ReadString();
        SendTime = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ChatNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ChatNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>50001 FriendAdd -> Game（服务器目标 Game）。</summary>
public sealed class FriendAdd : IMessage
{
    public const int MsgId = 50001;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string TargetUniqueId = "";
    public string Remark = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(TargetUniqueId);
        w.WriteString(Remark);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        TargetUniqueId = r.ReadString();
        Remark = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendAdd Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendAdd(); m.ReadBody(r); return m;
    }
}

/// <summary>50002 FriendAddResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendAddResult : IMessage
{
    public const int MsgId = 50002;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendAddResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendAddResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50003 FriendRemove -> Game（服务器目标 Game）。</summary>
public sealed class FriendRemove : IMessage
{
    public const int MsgId = 50003;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string FriendUniqueId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(FriendUniqueId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        FriendUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendRemove Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendRemove(); m.ReadBody(r); return m;
    }
}

/// <summary>50004 FriendRemoveResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendRemoveResult : IMessage
{
    public const int MsgId = 50004;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendRemoveResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendRemoveResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50005 FriendSetRemark -> Game（服务器目标 Game）。</summary>
public sealed class FriendSetRemark : IMessage
{
    public const int MsgId = 50005;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string FriendUniqueId = "";
    public string Remark = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(FriendUniqueId);
        w.WriteString(Remark);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        FriendUniqueId = r.ReadString();
        Remark = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendSetRemark Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendSetRemark(); m.ReadBody(r); return m;
    }
}

/// <summary>50006 FriendSetRemarkResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendSetRemarkResult : IMessage
{
    public const int MsgId = 50006;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendSetRemarkResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendSetRemarkResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50007 FriendGetList -> Game（服务器目标 Game）。</summary>
public sealed class FriendGetList : IMessage
{
    public const int MsgId = 50007;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(0); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendGetList Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendGetList(); m.ReadBody(r); return m;
    }
}

/// <summary>50008 FriendListResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendListResult : IMessage
{
    public const int MsgId = 50008;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public List<FriendInfo> Friends = new List<FriendInfo>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount(Friends.Count); foreach (var it in Friends) { w.WriteHeader(5); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int _n = r.ReadCount(); Friends = new List<FriendInfo>(); for (int _i = 0; _i < _n; _i++) { var _e = new FriendInfo(); r.SkipHeader(); _e.ReadBody(r); Friends.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendListResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendListResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50009 FriendInviteGame -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGame : IMessage
{
    public const int MsgId = 50009;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string FriendUniqueId = "";
    public string RoomId = "";
    public string SceneType = "";
    public string RoomName = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(4); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(FriendUniqueId);
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        FriendUniqueId = r.ReadString();
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGame Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGame(); m.ReadBody(r); return m;
    }
}

/// <summary>50010 FriendInviteGameResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGameResult : IMessage
{
    public const int MsgId = 50010;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGameResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGameResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50011 FriendInviteGameNotify -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGameNotify : IMessage
{
    public const int MsgId = 50011;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string InviterUniqueId = "";
    public string InviterNickname = "";
    public string RoomId = "";
    public string SceneType = "";
    public string RoomName = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(5); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(InviterUniqueId);
        w.WriteString(InviterNickname);
        w.WriteString(RoomId);
        w.WriteString(SceneType);
        w.WriteString(RoomName);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        InviterUniqueId = r.ReadString();
        InviterNickname = r.ReadString();
        RoomId = r.ReadString();
        SceneType = r.ReadString();
        RoomName = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGameNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGameNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>50012 BlacklistAdd -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistAdd : IMessage
{
    public const int MsgId = 50012;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string TargetUniqueId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(TargetUniqueId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        TargetUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistAdd Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistAdd(); m.ReadBody(r); return m;
    }
}

/// <summary>50013 BlacklistAddResult -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistAddResult : IMessage
{
    public const int MsgId = 50013;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistAddResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistAddResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50014 BlacklistRemove -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistRemove : IMessage
{
    public const int MsgId = 50014;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string TargetUniqueId = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(TargetUniqueId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        TargetUniqueId = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistRemove Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistRemove(); m.ReadBody(r); return m;
    }
}

/// <summary>50015 BlacklistRemoveResult -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistRemoveResult : IMessage
{
    public const int MsgId = 50015;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistRemoveResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistRemoveResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50016 BlacklistGetList -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistGetList : IMessage
{
    public const int MsgId = 50016;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(0); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistGetList Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistGetList(); m.ReadBody(r); return m;
    }
}

/// <summary>50017 BlacklistListResult -> Game（服务器目标 Game）。</summary>
public sealed class BlacklistListResult : IMessage
{
    public const int MsgId = 50017;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public List<BlacklistInfo> Blacklists = new List<BlacklistInfo>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount(Blacklists.Count); foreach (var it in Blacklists) { w.WriteHeader(4); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int _n = r.ReadCount(); Blacklists = new List<BlacklistInfo>(); for (int _i = 0; _i < _n; _i++) { var _e = new BlacklistInfo(); r.SkipHeader(); _e.ReadBody(r); Blacklists.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static BlacklistListResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new BlacklistListResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50018 FriendApply -> Game（服务器目标 Game）。</summary>
public sealed class FriendApply : IMessage
{
    public const int MsgId = 50018;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string TargetUniqueId = "";
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(TargetUniqueId);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        TargetUniqueId = r.ReadString();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApply Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApply(); m.ReadBody(r); return m;
    }
}

/// <summary>50019 FriendApplyResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyResult : IMessage
{
    public const int MsgId = 50019;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50020 FriendApplyNotify -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyNotify : IMessage
{
    public const int MsgId = 50020;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public long ApplyId = 0L;
    public int RequesterUserId = 0;
    public string RequesterUniqueId = "";
    public string RequesterNickname = "";
    public string Message = "";
    public long CreateTimeUtc = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(6); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(ApplyId);
        w.WriteI32(RequesterUserId);
        w.WriteString(RequesterUniqueId);
        w.WriteString(RequesterNickname);
        w.WriteString(Message);
        w.WriteI64(CreateTimeUtc);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        ApplyId = r.ReadI64();
        RequesterUserId = r.ReadI32();
        RequesterUniqueId = r.ReadString();
        RequesterNickname = r.ReadString();
        Message = r.ReadString();
        CreateTimeUtc = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>50021 FriendApplyList -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyList : IMessage
{
    public const int MsgId = 50021;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(0); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyList Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyList(); m.ReadBody(r); return m;
    }
}

/// <summary>50022 FriendApplyListResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyListResult : IMessage
{
    public const int MsgId = 50022;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public List<FriendApplyInfo> Applies = new List<FriendApplyInfo>();

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteCount(Applies.Count); foreach (var it in Applies) { w.WriteHeader(7); it.WriteBody(w); }
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        int _n = r.ReadCount(); Applies = new List<FriendApplyInfo>(); for (int _i = 0; _i < _n; _i++) { var _e = new FriendApplyInfo(); r.SkipHeader(); _e.ReadBody(r); Applies.Add(_e); }
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyListResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyListResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50023 FriendApplyHandle -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyHandle : IMessage
{
    public const int MsgId = 50023;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public long ApplyId = 0L;
    public bool Accept = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI64(ApplyId);
        w.WriteBool(Accept);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        ApplyId = r.ReadI64();
        Accept = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyHandle Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyHandle(); m.ReadBody(r); return m;
    }
}

/// <summary>50024 FriendApplyHandleResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendApplyHandleResult : IMessage
{
    public const int MsgId = 50024;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendApplyHandleResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendApplyHandleResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50025 FriendOnlineStatusNotify -> Game（服务器目标 Game）。</summary>
public sealed class FriendOnlineStatusNotify : IMessage
{
    public const int MsgId = 50025;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public int UserId = 0;
    public string UniqueId = "";
    public bool IsOnline = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(UserId);
        w.WriteString(UniqueId);
        w.WriteBool(IsOnline);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        UserId = r.ReadI32();
        UniqueId = r.ReadString();
        IsOnline = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendOnlineStatusNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendOnlineStatusNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>50026 FriendInviteGameAck -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGameAck : IMessage
{
    public const int MsgId = 50026;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string InviterUniqueId = "";
    public string RoomId = "";
    public bool Accept = false;
    public string Reason = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(4); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(InviterUniqueId);
        w.WriteString(RoomId);
        w.WriteBool(Accept);
        w.WriteString(Reason);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        InviterUniqueId = r.ReadString();
        RoomId = r.ReadString();
        Accept = r.ReadBool();
        Reason = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGameAck Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGameAck(); m.ReadBody(r); return m;
    }
}

/// <summary>50027 FriendInviteGameAckResult -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGameAckResult : IMessage
{
    public const int MsgId = 50027;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGameAckResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGameAckResult(); m.ReadBody(r); return m;
    }
}

/// <summary>50028 FriendInviteGameAckNotify -> Game（服务器目标 Game）。</summary>
public sealed class FriendInviteGameAckNotify : IMessage
{
    public const int MsgId = 50028;
    public const string Target = "Game";
    int IMessage.MessageId => MsgId;
    public string InviteeUniqueId = "";
    public string InviteeNickname = "";
    public string RoomId = "";
    public bool Accept = false;
    public string Reason = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(5); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(InviteeUniqueId);
        w.WriteString(InviteeNickname);
        w.WriteString(RoomId);
        w.WriteBool(Accept);
        w.WriteString(Reason);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        InviteeUniqueId = r.ReadString();
        InviteeNickname = r.ReadString();
        RoomId = r.ReadString();
        Accept = r.ReadBool();
        Reason = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FriendInviteGameAckNotify Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FriendInviteGameAckNotify(); m.ReadBody(r); return m;
    }
}

/// <summary>10001 Login -> Login（服务器目标 Login）。</summary>
public sealed class Login : IMessage
{
    public const int MsgId = 10001;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public string Account = "";
    public string Password = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(Account);
        w.WriteString(Password);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Account = r.ReadString();
        Password = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static Login Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new Login(); m.ReadBody(r); return m;
    }
}

/// <summary>10002 LoginResult -> Login（服务器目标 Login）。</summary>
public sealed class LoginResult : IMessage
{
    public const int MsgId = 10002;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";
    public int UserId = 0;
    public string Token = "";
    public string UniqueId = "";
    public string Nickname = "";
    public string Email = "";
    public long LastLoginTime = 0L;
    public int LoginCount = 0;
    public bool IsAdmin = false;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(10); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
        w.WriteI32(UserId);
        w.WriteString(Token);
        w.WriteString(UniqueId);
        w.WriteString(Nickname);
        w.WriteString(Email);
        w.WriteI64(LastLoginTime);
        w.WriteI32(LoginCount);
        w.WriteBool(IsAdmin);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
        UserId = r.ReadI32();
        Token = r.ReadString();
        UniqueId = r.ReadString();
        Nickname = r.ReadString();
        Email = r.ReadString();
        LastLoginTime = r.ReadI64();
        LoginCount = r.ReadI32();
        IsAdmin = r.ReadBool();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static LoginResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new LoginResult(); m.ReadBody(r); return m;
    }
}

/// <summary>10003 Register -> Login（服务器目标 Login）。</summary>
public sealed class Register : IMessage
{
    public const int MsgId = 10003;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public string Account = "";
    public string Password = "";
    public string Nickname = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(Account);
        w.WriteString(Password);
        w.WriteString(Nickname);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Account = r.ReadString();
        Password = r.ReadString();
        Nickname = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static Register Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new Register(); m.ReadBody(r); return m;
    }
}

/// <summary>10004 RegisterResult -> Login（服务器目标 Login）。</summary>
public sealed class RegisterResult : IMessage
{
    public const int MsgId = 10004;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static RegisterResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new RegisterResult(); m.ReadBody(r); return m;
    }
}

/// <summary>10005 Logout -> Login（服务器目标 Login）。</summary>
public sealed class Logout : IMessage
{
    public const int MsgId = 10005;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public int UserId = 0;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(1); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(UserId);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        UserId = r.ReadI32();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static Logout Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new Logout(); m.ReadBody(r); return m;
    }
}

/// <summary>10006 LogoutResult -> Login（服务器目标 Login）。</summary>
public sealed class LogoutResult : IMessage
{
    public const int MsgId = 10006;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static LogoutResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new LogoutResult(); m.ReadBody(r); return m;
    }
}

/// <summary>10007 ResetPassword -> Login（服务器目标 Login）。</summary>
public sealed class ResetPassword : IMessage
{
    public const int MsgId = 10007;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public string Account = "";
    public string OldPassword = "";
    public string NewPassword = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(3); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(Account);
        w.WriteString(OldPassword);
        w.WriteString(NewPassword);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Account = r.ReadString();
        OldPassword = r.ReadString();
        NewPassword = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ResetPassword Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ResetPassword(); m.ReadBody(r); return m;
    }
}

/// <summary>10008 ResetPasswordResult -> Login（服务器目标 Login）。</summary>
public sealed class ResetPasswordResult : IMessage
{
    public const int MsgId = 10008;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static ResetPasswordResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new ResetPasswordResult(); m.ReadBody(r); return m;
    }
}

/// <summary>10009 UpdateNickname -> Login（服务器目标 Login）。</summary>
public sealed class UpdateNickname : IMessage
{
    public const int MsgId = 10009;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public int UserId = 0;
    public string NewNickname = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteI32(UserId);
        w.WriteString(NewNickname);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        UserId = r.ReadI32();
        NewNickname = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static UpdateNickname Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new UpdateNickname(); m.ReadBody(r); return m;
    }
}

/// <summary>10010 UpdateNicknameResult -> Login（服务器目标 Login）。</summary>
public sealed class UpdateNicknameResult : IMessage
{
    public const int MsgId = 10010;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static UpdateNicknameResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new UpdateNicknameResult(); m.ReadBody(r); return m;
    }
}

/// <summary>10011 KickedOff -> Login（服务器目标 Login）。</summary>
public sealed class KickedOff : IMessage
{
    public const int MsgId = 10011;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public string Reason = "";
    public long Time = 0L;

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(Reason);
        w.WriteI64(Time);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Reason = r.ReadString();
        Time = r.ReadI64();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static KickedOff Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new KickedOff(); m.ReadBody(r); return m;
    }
}

/// <summary>10012 FindPasswordWithCode -> Login（服务器目标 Login）。</summary>
public sealed class FindPasswordWithCode : IMessage
{
    public const int MsgId = 10012;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public string Account = "";
    public string Email = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteString(Account);
        w.WriteString(Email);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Account = r.ReadString();
        Email = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FindPasswordWithCode Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FindPasswordWithCode(); m.ReadBody(r); return m;
    }
}

/// <summary>10013 FindPasswordWithCodeResult -> Login（服务器目标 Login）。</summary>
public sealed class FindPasswordWithCodeResult : IMessage
{
    public const int MsgId = 10013;
    public const string Target = "Login";
    int IMessage.MessageId => MsgId;
    public bool Success = false;
    public string Message = "";

    /// <summary>序列化为完整负载（对象头字节 + 成员数据），不含帧头。</summary>
    public byte[] Serialize()
    {
        var w = new MpWriter(); w.WriteHeader(2); WriteBody(w); return w.ToArray();
    }

    /// <summary>写入成员数据（不含对象头字节）。</summary>
    public void WriteBody(MpWriter w)
    {
        w.WriteBool(Success);
        w.WriteString(Message);
    }

    /// <summary>读取成员数据（不含对象头字节）。</summary>
    public void ReadBody(MpReader r)
    {
        Success = r.ReadBool();
        Message = r.ReadString();
    }

    /// <summary>从负载反序列化（跳过对象头字节后读成员数据）。</summary>
    public static FindPasswordWithCodeResult Deserialize(byte[] data)
    {
        var r = new MpReader(data); r.SkipHeader();
        var m = new FindPasswordWithCodeResult(); m.ReadBody(r); return m;
    }
}

