<?xml version="1.0" encoding="utf-8"?>
<!--
    DB 服务器协议定义（已迁移，占位文件）
    ============================================================
    本 .def 的消息/结构体已全部迁移到方案 A：Framework/Framework.Protocol/Messages/DbMessages.cs，
    以 C# [GameMessage]/[GameStruct] 声明，由 Framework.Protocol.Generator 源生成器在编译期生成
    MessageIds / RouterTable / IGameMessage 管线 / ProtocolManifest.json。

    保留本文件的目的：
      1. 记录 DB 服链路的 ID 段约定（1000-1999 内部服务通信；1000-1019 请求，1100-1119 回执）；
      2. 保持 Protocol/defs/ 目录的协议分段结构。
    DB 消息全部为内部消息（target=Db），不进入客户端清单。
-->
<Protocol name="Db" version="1">
  <!-- 已迁移到 [GameMessage]/[GameStruct]，见 Framework.Protocol/Messages/DbMessages.cs -->
</Protocol>
